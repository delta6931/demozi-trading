# Project Memory: MegaStandard

This document contains a summary of the project state, goals, and technical details for the "MegaStandard" website project as of March 9, 2026. This is intended to provide context for AI assistants like Gemini.

## 1. Project Overview
- **Project Name:** MegaStandard
- **Core Objective:** Developing a professional, high-performance product catalog website for industrial equipment, spare parts, and specific industrial brands.
- **Target Audience:** Industrial clients looking for specific parts and brands.

## 2. Technical Stack
- **Frontend:** Vanilla HTML5, Vanilla CSS3 (custom styles in `style.css`), and Vanilla JavaScript.
- **Data Architecture:** Products, brands, and spare parts are managed via JavaScript data files (`products-data.js`, `brands-data.js`, `spare-parts-data.js`) for fast, client-side filtering and search.
- **Scripting & Automation:** 
    - **Python:** Used for data parsing, encoding fixes, and complex transformations (e.g., `parse_wix_csv.py`, `add_fisher.py`, `update_brands.py`).
    - **PowerShell:** Used for system-level automation, file management, and image processing (e.g., `remove_bg.ps1`, `merge_products.ps1`).
    - **i18n:** A custom internationalization system (`i18n.js`) is implemented to support multiple languages.

## 3. Current Progress & Recent Milestones
- **Wix Integration:** Successfully parsed a CSV with ~5,000 products from Wix and integrated them into the local JS database (`products-data.js`).
- **Product Catalog Enhancements:** 
    - Products and brands are now clickable.
    - Individual product detail pages (`product-details.html/js`) and brand profile pages (`brand-profile.html`) have been created.
    - SEO-friendly descriptions have been generated for core items.
- **Spare Parts Management:** 
    - A dedicated spare parts catalog is being built.
    - Working on ensuring all 72 expected spare parts are correctly represented in the database.
- **Visuals & Assets:** 
    - Implemented scripts for image background removal (`remove_bg.ps1`) to ensure a premium look.
    - Image upscaling scripts (`upscale.py`) were introduced to handle lower-quality assets.
- **SEO & Search:** 
    - Search logic implemented in `products-logic.js` and `brands-logic.js`.
    - `sitemap.xml` and `robots.txt` are configured.

## 4. Key Components & Files
- `index.html`: Main landing page.
- `products-data.js`: The heart of the product catalog (~1.7MB of data).
- `i18n.js`: Localization logic.
- `style.css`: The primary design system.
- `remove_bg.ps1`: Automation script for cleaning up product images.

## 5. Ongoing Tasks & Future Goals
- Completing the full integration of the remaining spare parts.
- Refining the "Related Products" logic on detail pages.
- Finalizing the background removal process for the full catalog assets.
- Deployment planning for a fast-loading industrial storefront.

---
*Generated by Antigravity AI Assistant.*
